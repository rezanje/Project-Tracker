export default function Footer() {
  return (
    <footer className="mt-12 px-6 py-8 text-center text-[12.5px] text-[var(--ink3)]">
      <span className="font-bold text-[var(--ink2)]">Rakit</span> —
      track your work, share the progress.
    </footer>
  )
}

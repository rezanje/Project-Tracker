# Public vars — safe to expose to browser bundle (VITE_ prefix required)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only secrets — must NOT be VITE_-prefixed
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
APP_BASE_URL=http://localhost:3000
